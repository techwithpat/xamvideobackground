<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:VideoBackground" 
             x:Class="VideoBackground.MainPage" 
             BackgroundColor="Black">
    <Grid>
        <local:VideoPlayer Source="Hway.mp4" />
        <StackLayout VerticalOptions="End" Padding="20" Margin="10" Spacing="10">
            <Label Text="Welcome" 
                   HorizontalOptions="Center" 
                   TextColor="White" 
                   FontSize="Large" 
                   FontAttributes="Bold"  />
            <Button Text="Connexion" BackgroundColor="White" />
            <Button Text="Help" BackgroundColor="White" />
        </StackLayout>
    </Grid>
</ContentPage>